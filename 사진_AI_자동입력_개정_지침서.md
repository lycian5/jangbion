# 장비온 사진 AI 자동입력 기능 최종 지침서

작성일: 2026-08-12
대상: 장비온 PWA v3.6.13
결정: **1차는 운행 화면의 계기판 시간계·거리계 제안 기능만 구현한다. 작업확인서 분석은 보류한다.**

## 1. 목표와 원칙

사용자가 계기판 사진을 고르고 필요한 영역을 자르면 AI가 누적 시간계와 거리계를 읽어 운행 입력폼의 `hourMeter`, `odometer`에 **제안값**을 채운다. 화면에 표시된 날짜는 기록 날짜를 바꾸지 않는 참고 정보로만 판정한다. 사용자는 값을 확인·수정한 뒤 기존 `사용 기록 저장` 버튼으로 저장한다.

```text
사진 촬영/선택 → 계기판 영역 자르기 → 브라우저 압축·미리보기 → 분석 API → 검증 → 제안값 표시 → 사용자 확인 → 기존 저장
```

다음은 금지한다.

- 분석 결과로 IndexedDB 또는 운행 기록을 직접 저장
- 사진만으로 장비명·날짜·작업 내용을 추정해 확정 입력
- API 키를 `index.html`, `app.js`, 브라우저 저장소, Git에 포함

AI 분석 실패, 오프라인, API 미설정 상태에서도 사진 첨부·수동 입력·기존 저장은 항상 정상 동작해야 한다.

## 2. 현재 앱과의 정합성

현재 앱은 Vercel 정적 PWA이며, 사진과 기록은 IndexedDB의 `collections`·`photos` 저장소에 보관한다. 운행 기록의 실제 필드는 `hourMeter`, `odometer`, `photo`이고, 이전 날짜의 누적 계기값과 비교해 사용 시간·거리를 계산한다.

재사용할 구현:

- `inp-usage-photo` 사진 선택, 압축, 미리보기
- `currentUsagePhoto`와 운행 기록의 `photo`
- 이전 계기값보다 작을 때의 저장 전 확인
- JSON 백업·복원과 사진 분리 보관

신규로 필요한 것은 Vercel 서버 함수, 분석 결과 패널, 분석 메타데이터뿐이다. 별도 운행 테이블이나 사진 저장 서버는 1차에 만들지 않는다.

## 3. 1차 기능 범위

지원 필드와 역할은 아래로 제한한다.

| 필드 | 역할 |
| --- | --- |
| `hourMeter` | 계기판에서 읽은 누적 시간계(HR/h) 제안값; 1차 최우선 필드 |
| `odometer` | 계기판에서 읽은 누적 거리계(km) 제안값; 1차 최우선 필드 |
| `displayDate` | 계기판 화면 날짜; 선택한 기록 날짜와의 불일치 확인용 참고값 |
| `rawText` | OCR/모델이 읽은 근거 텍스트 |
| `confidence` | 장비온 검증 점수(0~100) |
| `needsReview` | 자동 반영 금지 또는 재확인 필요 여부 |

작업확인서·작업일보 분석은 보류하며 1차·후속 범위에 포함하지 않는다. 주유 영수증, 정비 내역서, 현장 사진 의미 분석도 별도 결정 전까지 진행하지 않는다.

## 4. 서버 경계와 API 계약

브라우저는 사진을 분석 API에 보내고, API만 공급자 비밀키를 읽는다.

```text
app.js → POST /api/ai/analyze-image → OCR/비전 공급자 → 규칙 검증 결과 → app.js
```

Vercel 배포용 서버 함수는 `api/ai/analyze-image.js`에 둔다. 요청에는 압축된 사진만 포함하며, API는 이미지 MIME 형식·최대 크기·허용 컨텍스트를 검사한다. 원본 사진을 별도 서버 저장소에 보관하지 않는다.

요청 예시:

```json
{
  "context": "usage",
  "equipmentId": "eq_001",
  "date": "2026-08-12",
  "image": "data:image/webp;base64,..."
}
```

- 1차 허용 `context`는 `usage`만이다.
- 클라이언트의 `equipmentId`, `date`, `confidence`는 신뢰하지 않는다. 기록 저장은 여전히 클라이언트의 기존 검증과 사용자의 확정 절차를 거친다.
- API는 타임아웃을 짧게 두고, 실패 시 구조화된 오류만 돌려준다.
- 공개 배포 API에는 호출 횟수·사진 크기 제한과 악용 방지가 출시 요건이다. 사용자 인증을 도입하기 전에는 IP 기준 제한과 서버 측 비용 한도를 둔다.

응답은 공급자 응답을 그대로 노출하지 않고 아래 형식으로 정규화한다.

```json
{
  "type": "meter",
  "fields": { "hourMeter": 3441.3, "odometer": 7618, "displayDate": "2026-08-11" },
  "fieldSources": { "hourMeter": "confirmed", "odometer": "confirmed", "displayDate": "confirmed" },
  "fieldConfidence": { "hourMeter": 98, "odometer": 96, "displayDate": 91 },
  "needsReview": false,
  "rawText": "3441.3 Hr · 7618 km · 2026.08.11",
  "provider": "server-configured",
  "model": "server-configured"
}
```

`fieldSources`는 `confirmed`, `suggested`, `unknown`만 허용한다. 시간계·거리계·화면 날짜는 각각 별도 신뢰도를 갖는다. 모델명·공급자 키·원시 오류는 서버 로그 외에 브라우저로 보내지 않는다.

## 5. 분석 공급자와 개인정보

문자·숫자 판독에는 OCR 또는 이미지 입력과 JSON 출력을 지원하는 서버 측 모델을 사용한다. 서버는 `AI_PROVIDER`로 공급자를 선택하며, `openai`, `nvidia`, `openai-compatible`를 지원한다. 브라우저는 공급자와 무관하게 동일한 분석 응답만 받는다. OpenAI를 사용할 경우 Responses API와 이미지 입력·구조화된 출력을 지원하는 모델을 서버 설정으로 선택한다. [공식 모델 문서](https://developers.openai.com/api/docs/models/gpt-5)는 해당 기능 지원 여부를 확인하는 기준이다.

공급자 키(`OPENAI_API_KEY`, `NVIDIA_API_KEY`, 또는 `AI_API_KEY`)는 Vercel 환경변수에만 저장한다. 이 기능은 사진을 제3자 AI 공급자에 전송하므로, 화면과 개인정보 처리 고지에 전송 목적·보관 정책·사용자 선택권을 명시한다. 특히 OpenAI Responses API는 기본적으로 응답 애플리케이션 상태가 보존될 수 있고, 별도의 보존 관리 조건이 적용될 수 있다. 서버 요청에는 가능한 경우 `store: false`를 지정하되, 이것만으로 모든 보존이 사라진다고 안내하지 않는다. [공식 데이터 보존 문서](https://developers.openai.com/api/docs/guides/your-data#default-usage-policies-by-endpoint)를 기준으로 실제 계정 설정을 확인한다.

분석 이력이나 사용자 수정값을 외부 전송·모델 학습에 사용하지 않는다. 향후 품질 개선 목적으로 사용하려면 별도 동의와 보관 정책을 먼저 마련한다.

## 6. 검증 및 UX 정책

모델의 신뢰도만 믿지 않고 앱 규칙으로 재평가한다.

| 조건 | 처리 |
| --- | --- |
| `HR`, `h`, `시간계`와 연결된 유효 숫자 | 시간계 신뢰도 가점 |
| `km`, 거리계와 연결된 유효 숫자 | 거리계 신뢰도 가점 |
| 숫자 형식과 소수점이 정상 | 가점 |
| 이전 시간계 이상 | 가점 |
| 이전 거리계 이상 | 가점 |
| 이전 시간계보다 작음 | `needsReview = true` |
| 이전 거리계보다 작음 | `needsReview = true` |
| 이전 값과의 차이가 24시간 초과 | `needsReview = true` |
| 숫자 후보가 둘 이상 | 자동 반영 금지, 후보 표시 |
| 화면 날짜와 선택한 기록 날짜가 다름 | 자동 변경 금지, 불일치 안내 표시 |
| 사진이 흐리거나 분석 실패 | 수동 입력 유지 |

- 95~100: 해당 필드(`hourMeter` 또는 `odometer`)를 채우고 `AI 자동입력` 표시
- 75~94: 해당 필드를 채우고 `확인 필요` 표시
- 0~74 또는 `needsReview=true`: 해당 필드는 바꾸지 않고 후보값과 재촬영 안내 표시
- `displayDate`: 어떤 신뢰도여도 앱의 선택 날짜를 바꾸지 않고, 다를 때만 `사진 날짜와 기록 날짜가 다릅니다`를 표시

사용자가 AI가 채운 값을 수정하면 `사용자 수정`으로 기록한다. 사진을 교체하거나 값을 직접 지우면 해당 제안은 적용 전 상태로 되돌린다.

화면 흐름은 다음처럼 단순하게 유지한다.

```text
사진 선택 → 계기판 영역 자르기 → “계기판 숫자 확인 중…” → 제안 패널
현재 시간계 [3441.3] h · AI 자동입력
현재 거리계 [7618] km · AI 자동입력
사진 날짜 2026-08-11 · 선택한 기록 날짜와 일치
이전 시간계·거리계 대비 사용량 표시
```

### 사진 자르기

가능하다. 촬영 또는 갤러리 선택 직후 브라우저에서 자르기 화면을 열고, 사용자가 계기판 LCD 영역만 드래그해 확정한다. 자른 이미지만 800px 이내로 압축해 분석 API에 보내며, 확정한 자른 이미지를 기존 증빙 사진으로 저장한다.

- 기본 자르기 비율은 자유 선택으로 한다. 장비별 화면 비율이 다르므로 고정 비율을 강제하지 않는다.
- 자르기 화면에는 `다시 선택`, `원본으로 되돌리기`, `자르기 적용`을 제공한다.
- 분석용 전송과 로컬 저장 모두 자른 이미지를 사용해 화면 밖 개인정보·주변 물체 전송을 줄인다.
- 자르기를 취소하면 원본 전체 사진을 유지하고, AI 분석만 건너뛴다.

## 7. IndexedDB 및 백업 변경

분석 메타데이터는 현재 DB에 아래 두 컬렉션으로 추가한다.

`aiImageAnalyses`:

```json
{
  "id": "analysis_...",
  "equipmentId": "eq_001",
  "recordType": "usage",
  "photoRef": "photo:...",
  "rawText": "1284.6 HR",
  "extractedData": { "hourMeter": 3441.3, "odometer": 7618, "displayDate": "2026-08-11" },
  "fieldConfidence": { "hourMeter": 98, "odometer": 96, "displayDate": 91 },
  "needsReview": false,
  "userConfirmed": true,
  "createdAt": "2026-08-12T...",
  "confirmedAt": "2026-08-12T..."
}
```

`aiCorrections`:

```json
{
  "id": "correction_...",
  "analysisId": "analysis_...",
  "fieldName": "hourMeter",
  "aiValue": 1284.5,
  "userValue": 1284.8,
  "createdAt": "2026-08-12T..."
}
```

구현 시 `DB_VERSION`, `collectionKeys()`, `migrateDatabase()`, 백업·복원과 사진 참조 정리 로직을 함께 갱신한다. 이 네 지점 중 하나라도 빠지면 재시작·백업 복원 때 분석 이력이 유실될 수 있다.

## 8. 구현 순서와 완료 기준

1. Vercel 환경변수, 서버 함수, 요청 제한·오류 형식 마련
2. 공급자 호출과 정규화된 응답 스키마 구현
3. 운행 사진 선택 뒤 계기판 영역 자르기와 자른 사진 미리보기 추가
4. 분석 상태·결과 패널, `hourMeter`·`odometer` 제안 반영, 이전 계기값·24시간 검증 연결
5. 분석·수정 이력, DB 마이그레이션, 백업·복원 반영
6. 아래 시나리오로 브라우저 회귀 검증

필수 검증 시나리오:

- 시간계 `3441.3 Hr`, 거리계 `7618 km`, 화면 날짜 `2026-08-11`이 함께 보이는 정상 계기판
- 자른 사진, 회전 사진, 흐린 사진, 자르기 취소
- 숫자 후보 둘 이상, 이전 값보다 작은 값, 24시간 초과 차이, 사진 날짜 불일치
- API 키 미설정, 오프라인, 타임아웃, 공급자 오류
- AI 값을 수정한 뒤 저장, AI 값을 적용하지 않은 수동 저장
- 사진 정리·JSON 백업·복원 후 분석 이력과 사진 참조

1차는 다음 조건을 모두 만족할 때 완료다.

- 분석 결과가 기록을 직접 저장하지 않는다.
- 위험하거나 불확실한 값은 자동 반영되지 않는다.
- AI 실패가 기존 입력·사진·저장을 막지 않는다.
- 키와 공급자 내부 정보가 브라우저·Git에 노출되지 않는다.
- 공개 API의 비용·악용 방지와 사진 전송 고지가 적용된다.

## 9. 2026-08-13 운영 업데이트 현황

### 배포와 공급자 전환

- Vercel Production 배포 보호의 Vercel Authentication을 해제해 현장 사용자가 앱과 분석 API에 접근할 수 있게 했다.
- NVIDIA 공급자에서 발생한 모델 종료(HTTP 410), 권한 거부(HTTP 403), 모델 서버 연결 실패를 확인했다.
- 현재 운영 공급자는 OpenAI다. Vercel Production 환경변수는 아래 값으로 설정한다.

```text
AI_PROVIDER=openai
OPENAI_API_KEY=<OpenAI API 키>
OPENAI_VISION_MODEL=gpt-5-mini
```

- `OPENAI_API_KEY`는 Vercel의 Sensitive 환경변수로만 보관한다. 브라우저 코드, Git 저장소, 작업 문서에 기록하지 않는다.
- 환경변수를 변경하면 최신 Production 배포를 Redeploy하여 새 값이 함수에 반영되도록 한다.

### API 안정화 반영 사항

- `api/ai/analyze-image.js`는 OpenAI Responses API의 `output_text`뿐 아니라 `output[].content[].text` 응답도 읽는다.
- 모델이 JSON 앞뒤에 설명을 덧붙여도 JSON 객체를 추출해 처리한다.
- 공급자 호출 실패 시 Vercel Function Logs에 공급자, 모델명, upstream 상태 코드, 오류 유형, 오류 메시지만 남긴다.
- API 키, Authorization 헤더, 사진 Base64 본문은 로그에 남기지 않는다.
- 현재 오류 진단 로그가 필요한 경우 Vercel Logs에서 `/api/ai/analyze-image` 요청을 열고 Function Logs를 확인한다.

### 현재 기본 사용 제한

| 항목 | 현재 제한 |
| --- | --- |
| 분석 요청 횟수 | 동일 IP 기준 10분당 12회 |
| 사진 데이터 크기 | Data URL 문자열 기준 최대 2,700,000자 |
| 허용 이미지 형식 | JPEG, PNG, WebP |
| 공급자 분석 시간 | 서버 14초, 브라우저 요청 15초 |
| 분석 대상 | 운행 기록의 계기판 사진(`usage`)만 허용 |

이 제한은 기본적인 오남용과 과도한 비용을 줄이기 위한 방어선이며, IP만으로는 사용자별 사용량을 정확히 관리할 수 없다.

### 권장 운영 제한

로그인 기능 도입 전에는 현재 IP 제한을 유지한다. 로그인 도입 후에는 아래 제한으로 전환한다.

| 구분 | 권장값 | 목적 |
| --- | --- | --- |
| 사용자별 일일 분석 횟수 | 10회 | 현장 사용량 관리 |
| 전체 일일 분석 횟수 | 100회 | 비용 상한 보호 |
| 비로그인 분석 | 차단 | 무단 API 사용 방지 |
| 관리자 AI On/Off | 제공 | 장애·비용 발생 시 즉시 중지 |
| 사용 이력 | 사용자·시각·성공 여부만 기록 | 사용량 감사 및 한도 집계 |

사진 원본이나 API 키는 사용 이력에 저장하지 않는다. 사진이 필요할 때만 기존 운행 기록의 사진 참조를 사용한다.

## 10. 로그인 사용자 제한 도입안: Supabase

### 선택안

Supabase Auth를 사용해 이메일·비밀번호 로그인 방식을 도입한다. Newsweaver와 같은 Supabase 계정 또는 조직에서 `jangbion` 전용 프로젝트를 새로 만들 수 있으며, 프로젝트가 다르면 사용자·DB·API 키는 서로 분리된다.

### 준비 항목

1. Supabase에서 `jangbion` 전용 프로젝트 생성
2. Authentication에서 Email/Password 로그인 활성화
3. 관리자 계정으로 작업자 이메일 초대, 비활성화, 비밀번호 재설정 관리
4. Vercel 환경변수 설정

```text
SUPABASE_URL=<프로젝트 URL>
SUPABASE_ANON_KEY=<공개 anon 키>
SUPABASE_SERVICE_ROLE_KEY=<서버 전용 service_role 키>
```

`SUPABASE_SERVICE_ROLE_KEY`는 서버 함수에서만 사용하며 `app.js`, `index.html`, 브라우저 환경변수에 노출하지 않는다.

### 구현 범위

- 로그인·로그아웃 화면과 세션 유지
- 로그인하지 않은 사용자의 사진 분석 버튼 및 API 호출 차단
- 서버 함수에서 Supabase Access Token 검증
- 사용자별 일일 분석 횟수 집계와 한도 초과 응답
- 관리자용 사용자 상태·분석 사용량 확인 기능

초기 도입 범위는 **로그인한 사용자만 AI 분석 가능**과 **사용자별 일일 횟수 제한**으로 한정한다. 기존 IndexedDB 운행 기록을 Supabase DB로 전체 이전하는 작업은 별도 결정 전까지 포함하지 않는다.

### 관리자 운영 흐름

```text
관리자 → Supabase에서 작업자 초대/비활성화
작업자 → 이메일·비밀번호로 로그인
앱 → Access Token과 함께 사진 분석 요청
서버 → 토큰 검증 → 일일 한도 확인 → OpenAI 분석 실행 → 사용량 기록
```

## 11. 배포 후 점검 절차

1. Vercel Environment Variables에서 `AI_PROVIDER`, `OPENAI_API_KEY`, `OPENAI_VISION_MODEL`의 Production 적용 여부를 확인한다.
2. 최신 Production 배포가 Ready인지 확인한다.
3. 계기판 숫자와 시간계·거리계가 잘 보이도록 자른 사진으로 분석을 한 번 실행한다.
4. 성공 시 시간계·거리계의 신뢰도가 75 이상일 때만 자동입력되는지 확인한다.
5. 실패 시 앱의 AI 상태 문구와 Vercel Logs의 `/api/ai/analyze-image` Function Logs를 함께 확인한다.
6. `503`은 공급자 설정 누락, `502`는 공급자 호출·응답 처리 실패 가능성을 우선 점검한다.
7. 분석 실패 시에도 사진 미리보기·수동 입력·운행 기록 저장이 계속 가능한지 확인한다.

## 12. 작업 이력 및 알림 표시 기준

### 장비 기록 이력의 작업 표시

- 장비 기록 이력에서 작업 항목의 상세 표시는 `메모`가 아니라 `작업 회사`를 사용한다.
- 작업 회사를 입력하지 않은 기존·신규 기록은 `작업 회사 미입력`으로 표시한다.
- 작업 메모는 기록 저장 데이터에 유지되며, 작업 이력의 대표 표시에만 사용하지 않는다.

### 알림 숫자의 의미

상단 및 하단의 알림 아이콘 숫자는 **현재 선택한 장비와 날짜에서 확인이 필요한 항목 수**다. 숫자는 최대 `9+`로 표시한다.

포함 항목:

- 운행 기록 미입력
- 작업 기록 미입력
- DPF·구리스·엔진오일·미션오일·대우오일·필터 교환·앞타이어 교체·뒷타이어 교체 정비 주기 정보 미입력 또는 점검 필요
- 미해결 고장으로 인한 운행 제한

정비 주기가 정상인 항목은 알림 목록에는 표시될 수 있지만, 빨간 알림 숫자에는 포함하지 않는다. 휴무·비운행 상태가 기록된 날에는 사용·작업 미입력 알림을 만들지 않는다.

## 13. 최종 운영 상태

- AI 계기판 분석: OpenAI `gpt-5-mini` 사용, 시간계·거리계만 조건부 자동입력
- 화면 날짜: 참고값으로만 판정하며 기록 날짜를 자동 변경하지 않음
- 작업확인서 분석: 보류, 자동 분석 대상에서 제외
- 사진 처리: 운행 사진 선택 후 계기판 영역을 자르고 분석 가능
- 안전장치: 신뢰도 75 미만, 이전 계기값보다 작은 값, 시간계 24시간 초과 증가는 자동입력하지 않음
- 현 사용 제한: IP 기준 10분 12회, 사진 크기·형식·분석 시간 제한 적용
- 다음 우선순위: Supabase Auth 도입 후 로그인 사용자만 분석 허용하고 사용자별 일일 한도를 적용
